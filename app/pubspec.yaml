name: meidan_app
description: Minimal Flutter UI for the Meidan cry alert prototype.
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  audioplayers: ^6.6.0
  flutter:
    sdk: flutter
  http: ^1.2.0

flutter:
  uses-material-design: true
  assets:
    - Assets/we.mp3
    - Assets/auto.mp3
    - Assets/warn.mp3
